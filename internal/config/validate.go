package config

// Bound checks of PRD §6.2 live here, and report every violation at once:
// the check command is worth far more when it lists the five faulty keys than
// when it stops at the first.
